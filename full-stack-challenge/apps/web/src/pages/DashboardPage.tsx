import { Box, Typography } from '@mui/material';

export default function DashboardPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h1">Painel</Typography>
      <Typography color="text.secondary">Em construção</Typography>
    </Box>
  );
}